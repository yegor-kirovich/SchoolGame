/// @description Вставьте описание здесь
// Вы можете записать свой код в этом редакторе
if say{
	if say and string_length(letters) != string_length(global.replica){
		letters = global.replica
	}
	else if string_length(letters) == string_length(global.replica){
		say = false
	}
}